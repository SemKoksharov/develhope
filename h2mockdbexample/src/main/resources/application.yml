spring:
  application.name: h2mockdbexample
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: testdb
    password: psw1234
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
